import 'package:flutter/material.dart';

class ${upperName}Page extends StatelessWidget {
  const ${upperName}Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}