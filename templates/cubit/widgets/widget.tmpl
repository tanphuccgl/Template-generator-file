import 'package:flutter/material.dart';

class ${upperName}Widget extends StatelessWidget {
  const ${upperName}Widget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}